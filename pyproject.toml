[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "femto-rul"
version = "0.1.0"
description = "FEMTO/PRONOSTIA bearing RUL prediction — data, features, and pipeline"
requires-python = ">=3.12.3"

[tool.setuptools.packages.find]
where = ["src"]
