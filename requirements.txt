# Week 1 — data foundation deps. Airflow/MLflow/XGBoost etc. get added in Week 2
# as the orchestration and modeling stages come online.
pandas==3.0.5
numpy==2.5.1
scipy==1.18.0
scikit-learn==1.9.0
matplotlib==3.11.1
jupyter==1.1.1
pytest==9.1.1
pyarrow==25.0.0
# Data versioning / MinIO S3 remote
dvc[s3]==3.67.1

# Experiment tracking / model selection stack.
# MLflow is aligned with the repository's existing MLflow server image.
mlflow-skinny==3.1.4
PyYAML==6.0.3
optuna==4.8.0
xgboost>=3.1,<4
lightgbm==4.6.0

# Phase 16/17 — inference telemetry + monitoring.
# psycopg2-binary version matches docker/mlflow/Dockerfile's pin, so the
# app and the mlflow container agree on driver behavior.
psycopg2-binary==2.9.10
# Verified against pandas==3.0.5/scikit-learn==1.9.0 above (dry-run install,
# 2026-08-14) — resolves cleanly, no downgrade needed.
evidently==0.7.21

# install this repo's own package (src/femto_rul) in editable mode
-e .
