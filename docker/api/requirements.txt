# Minimal deps for api/main.py — deliberately not the root requirements.txt,
# which also pulls jupyter/optuna/xgboost/lightgbm/dvc (unrelated to serving).
# Versions matched to the pins already used elsewhere in the repo.
fastapi==0.141.1
uvicorn==0.52.3
pydantic==2.13.4
mlflow-skinny==3.1.4
pandas==3.0.5
psycopg2-binary==2.9.10
boto3==1.39.3
